/**
 * Shared TypeScript types consumed by apps/admin, apps/web.
 *
 * These will be generated automatically from the NestJS OpenAPI spec
 * using openapi-typescript once apps/api has its first endpoints.
 *
 * Run `pnpm --filter @jobbees/types generate` to regenerate.
 */

export type Placeholder = {
  readme: 'Replace with generated types after apps/api scaffolding';
};
